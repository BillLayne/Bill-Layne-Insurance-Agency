// Minimal core script to avoid 404s and provide a place for shared enhancements
(function () {
  if (window.__APP_JS_LOADED__) return;
  window.__APP_JS_LOADED__ = true;

  document.addEventListener('DOMContentLoaded', function () {
    // No-op placeholder; keeps place for future shared code
  });
})();
